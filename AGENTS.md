# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This project was intentionally downgraded from SDK 57 to SDK 54 (see git history) to match the
Expo Go client version actually installed on the developer's device. Do not "helpfully" upgrade
expo/expo-router/etc. back to their latest majors without being asked — the versions pinned in
package.json are deliberate, not stale.
